#include <stdio.h>

/**
 * main - Prints a string to both console and files.
 *
 * Return: value 0.
 */

int main(void)
{
	
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
